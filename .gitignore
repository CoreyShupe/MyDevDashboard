/target
.env
*.log
.DS_Store

# Local scratch dir (review screenshots from `dev-dash shot`, etc). Contents ignored;
# the screenshots/ subfolder is kept via its .gitkeep so the path always exists.
/static/tmp/*
!/static/tmp/screenshots
/static/tmp/screenshots/*
!/static/tmp/screenshots/.gitkeep
